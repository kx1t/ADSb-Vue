__pycache__/
*.pyc
*.pyo
# local config — commit .env.example, not your real .env
.env

# per-deployment city labels — commit cities.local.json.example, not your real one
cities.local.json

# persistence store (lives on a mapped volume; never in the repo)
*.db
*.db-wal
*.db-shm
# the recommended bind-mount location (./data:/data) inside the repo folder
data/
